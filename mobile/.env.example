# Yerel geliştirme varsayılanı — iOS Simülatör host makineyle ağı paylaşır,
# bu yüzden `localhost` doğrudan çalışır. Android emülatörde `10.0.2.2`
# kullanın (EXPO_PUBLIC_API_URL=http://10.0.2.2:3000 ile ezin).
EXPO_PUBLIC_API_URL=http://localhost:3000
