"use client";

import { useEffect, useState } from "react";
import { useRouter } from "next/navigation";
import { CirclePause, CirclePlay, RotateCcw, ShieldAlert, SquareCheckBig } from "lucide-react";

type Cohort = { id: string; groupName: string; status: "DRAFT" | "ACTIVE" | "PAUSED" | "COMPLETED" | "ROLLED_BACK"; version: number; memberCount: number; coverage: { ADMIN: number; TEACHER: number; STUDENT: number; PARENT: number }; createdAt: string };

export function PilotRolloutControl({ groups, cohorts }: { groups: Array<{ id: string; name: string; detail: string }>; cohorts: Cohort[] }) {
  const router = useRouter(); const [rows, setRows] = useState(cohorts); const [groupId, setGroupId] = useState(groups[0]?.id || ""); const [busy, setBusy] = useState(false); const [message, setMessage] = useState(""); const [stopReason, setStopReason] = useState("OPERATIONAL");
  useEffect(() => setRows(cohorts), [cohorts]);
  async function create() { setBusy(true); setMessage(""); const response = await fetch("/api/panel/pilot-cohorts", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ groupId, requestKey: crypto.randomUUID() }) }); const body = await response.json().catch(() => ({})); setBusy(false); if (!response.ok) return setMessage(body.error || "Pilot taslağı oluşturulamadı."); const created = body.cohort; const coverage = created.members.reduce((counts: Cohort["coverage"], member: { role: keyof Cohort["coverage"] }) => ({ ...counts, [member.role]: counts[member.role] + 1 }), { ADMIN: 0, TEACHER: 0, STUDENT: 0, PARENT: 0 }); setRows((current) => [{ id: created.id, groupName: created.groupNameSnapshot, status: created.status, version: created.version, memberCount: created.members.length, coverage, createdAt: created.createdAt }, ...current]); setMessage("Dört rollü pilot taslağı oluşturuldu; aktivasyon kapılarını kontrol edin."); router.refresh(); }
  async function transition(cohort: Cohort, action: "ACTIVATE" | "PAUSE" | "RESUME" | "COMPLETE" | "ROLLBACK") { setBusy(true); setMessage(""); const response = await fetch(`/api/panel/pilot-cohorts/${cohort.id}`, { method: "PATCH", headers: { "content-type": "application/json" }, body: JSON.stringify({ action, expectedVersion: cohort.version, stopReason: ["PAUSE", "ROLLBACK"].includes(action) ? stopReason : undefined }) }); const body = await response.json().catch(() => ({})); setBusy(false); if (!response.ok) return setMessage(body.error || "Pilot durumu güncellenemedi."); setRows((current) => current.map((item) => item.id === cohort.id ? { ...item, status: body.cohort.status, version: body.cohort.version } : item)); setMessage(action === "ACTIVATE" || action === "RESUME" ? "Pilot erişimi etkinleştirildi." : action === "COMPLETE" ? "Pilot tamamlandı; kayıtlar korundu." : "Pilot erişimi güvenle durduruldu; kayıtlar korunuyor."); router.refresh(); }
  return <div className="grid gap-5 xl:grid-cols-[340px_minmax(0,1fr)]"><section className="panel-surface h-fit p-5"><h2 className="text-sm font-extrabold">Gruptan pilot kohortu oluştur</h2><p className="mt-2 text-xs leading-5 text-[var(--site-muted)]">Öğretmen, aktif öğrenciler, bağlı veliler ve işlemi yapan admin tek kohorta alınır. Eksik rol varsa pilot oluşmaz.</p><label className="mt-4 block text-xs font-extrabold">Aktif grup<select aria-label="Pilot grubu" className="panel-input mt-1" value={groupId} onChange={(event) => setGroupId(event.target.value)}><option value="">Grup seçin</option>{groups.map((group) => <option key={group.id} value={group.id}>{group.name} · {group.detail}</option>)}</select></label><button type="button" disabled={busy || !groupId} onClick={() => void create()} className="site-btn site-btn-primary site-btn-sm mt-4 w-full">Pilot taslağı oluştur</button><p className="mt-4 border-t border-[var(--site-line)] pt-4 text-[10px] leading-4 text-[var(--site-muted)]">Kohort oluşturmak erişimi açmaz. Aktivasyon; env, güvenlik, restore, dört rol ve SLO kapılarından geçer.</p></section><section><div className="flex flex-wrap items-end justify-between gap-3"><div><h2 className="text-sm font-extrabold">Yayın yaşam döngüsü</h2><p className="mt-1 text-xs text-[var(--site-muted)]">Sürüm kontrolü eski sekmenin yeni kararı ezmesini engeller.</p></div><label className="text-xs font-extrabold">Durdurma nedeni<select aria-label="Pilot durdurma nedeni" className="panel-input mt-1 w-auto" value={stopReason} onChange={(event) => setStopReason(event.target.value)}><option value="OPERATIONAL">Operasyon</option><option value="GUARDRAIL_BREACH">Guardrail ihlali</option><option value="SECURITY_INCIDENT">Güvenlik olayı</option><option value="DATA_QUALITY">Veri kalitesi</option></select></label></div>{message ? <p role="status" className="mt-3 rounded-2xl bg-[var(--brand-olive-soft)] p-3 text-xs font-bold text-[var(--brand-olive)]">{message}</p> : null}<div className="mt-3 space-y-3">{rows.map((cohort) => <article key={cohort.id} className="panel-surface p-5"><div className="flex flex-wrap items-start justify-between gap-3"><div><div className="flex items-center gap-2"><h3 className="text-sm font-extrabold">{cohort.groupName}</h3><span className={`rounded-full px-2 py-1 text-[10px] font-extrabold ${cohort.status === "ACTIVE" ? "bg-emerald-100 text-emerald-800" : cohort.status === "PAUSED" ? "bg-amber-100 text-amber-900" : "bg-slate-100 text-slate-700"}`}>{cohort.status}</span></div><p className="mt-2 text-xs text-[var(--site-muted)]">{cohort.memberCount} üye · Admin {cohort.coverage.ADMIN} · Öğretmen {cohort.coverage.TEACHER} · Öğrenci {cohort.coverage.STUDENT} · Veli {cohort.coverage.PARENT} · v{cohort.version}</p></div><div className="flex flex-wrap gap-2">{cohort.status === "DRAFT" ? <button disabled={busy} type="button" className="panel-quick-action panel-quick-action-primary" onClick={() => void transition(cohort, "ACTIVATE")}><CirclePlay size={14} /> Aktive et</button> : null}{cohort.status === "ACTIVE" ? <><button disabled={busy} type="button" className="panel-quick-action" onClick={() => void transition(cohort, "PAUSE")}><CirclePause size={14} /> Duraklat</button><button disabled={busy} type="button" className="panel-quick-action" onClick={() => void transition(cohort, "COMPLETE")}><SquareCheckBig size={14} /> Tamamla</button><button disabled={busy} type="button" className="panel-quick-action text-rose-700" onClick={() => void transition(cohort, "ROLLBACK")}><ShieldAlert size={14} /> Geri al</button></> : null}{cohort.status === "PAUSED" ? <><button disabled={busy} type="button" className="panel-quick-action panel-quick-action-primary" onClick={() => void transition(cohort, "RESUME")}><RotateCcw size={14} /> Sürdür</button><button disabled={busy} type="button" className="panel-quick-action text-rose-700" onClick={() => void transition(cohort, "ROLLBACK")}><ShieldAlert size={14} /> Geri al</button></> : null}</div></div></article>)}{!rows.length ? <p className="rounded-2xl border border-dashed border-[var(--site-line)] p-8 text-center text-sm text-[var(--site-muted)]">Henüz pilot kohortu yok.</p> : null}</div></section></div>;
}
