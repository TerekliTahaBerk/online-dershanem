"use client";

import { useState } from "react";
import { CalendarDays, Check, CircleAlert, ListChecks, RefreshCw, SlidersHorizontal } from "lucide-react";

type Preference = { availableDays: number[]; minutesPerDay: number; nextExamAt: string | null; examLabel: string | null; planningEnabled: boolean; overwhelmPulse: number | null };
type Task = { id: string; title: string; scheduledFor: string; durationMinutes: number; sourceType: "ASSIGNMENT" | "REVIEW" | "WEAK_OUTCOME" | "EXAM_PREP" | "RECOVERY"; reasonCode: "DUE_SOON" | "REVIEW_DUE" | "NEEDS_REVIEW" | "EXAM_APPROACHING" | "CAPACITY_BALANCE" | "MISSED_LESSON"; status: "PLANNED" | "DONE" | "SKIPPED" };
type Plan = { id: string; status: "DRAFT" | "APPROVED" | "CHANGE_REQUESTED" | "ARCHIVED"; version: number; capacityMinutes: number; tasks: Task[] };
const days = [{ id: 1, label: "Pzt" }, { id: 2, label: "Sal" }, { id: 3, label: "Çar" }, { id: 4, label: "Per" }, { id: 5, label: "Cum" }, { id: 6, label: "Cmt" }, { id: 7, label: "Paz" }];
const reasons: Record<Task["reasonCode"], string> = { DUE_SOON: "Son tarihi yaklaştığı için önce", REVIEW_DUE: "Hatırlama zamanı geldiği için", NEEDS_REVIEW: "Tekrar gerekli görüldüğü için", EXAM_APPROACHING: "Yaklaşan sınava küçük bir adım olduğu için", CAPACITY_BALANCE: "Günlük sürene uyduğu için", MISSED_LESSON: "Kaçırdığın dersin 72 saatlik küçük telafisi olduğu için" };
const date = new Intl.DateTimeFormat("tr-TR", { weekday: "long", day: "numeric", month: "short" });

export function StudentAdaptivePlan({ initialPreference, initialPlan }: { initialPreference: Preference; initialPlan: Plan | null }) {
  const [preference, setPreference] = useState(initialPreference); const [plan, setPlan] = useState(initialPlan); const [busy, setBusy] = useState(false); const [message, setMessage] = useState(""); const [changeCategory, setChangeCategory] = useState("TOO_MUCH");
  async function savePreference() { setBusy(true); setMessage(""); const response = await fetch("/api/panel/adaptive-plan/preferences", { method: "PATCH", headers: { "content-type": "application/json" }, body: JSON.stringify({ ...preference, nextExamAt: preference.nextExamAt ? new Date(`${preference.nextExamAt.slice(0, 10)}T12:00:00.000Z`).toISOString() : null, examLabel: preference.nextExamAt ? preference.examLabel || "OKUL SINAVI" : null }) }); const body = await response.json().catch(() => ({})); setBusy(false); setMessage(response.ok ? "Tercihlerin kaydedildi. Şimdi açıklanabilir öneriyi oluşturabilirsin." : body.error || "Tercihler kaydedilemedi."); }
  async function generate() { setBusy(true); setMessage(""); const response = await fetch("/api/panel/adaptive-plan/generate", { method: "POST" }); const body = await response.json().catch(() => ({})); setBusy(false); if (!response.ok) return setMessage(body.error || "Plan oluşturulamadı."); window.location.reload(); }
  async function complete(task: Task) { const response = await fetch(`/api/panel/adaptive-plan/tasks/${task.id}/complete`, { method: "POST" }); if (!response.ok) return setMessage("Görev tamamlanamadı."); setPlan((current) => current ? { ...current, tasks: current.tasks.map((item) => item.id === task.id ? { ...item, status: "DONE" } : item) } : current); }
  async function requestChange() { if (!plan) return; setBusy(true); const response = await fetch(`/api/panel/adaptive-plan/${plan.id}/request-change`, { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ category: changeCategory, expectedVersion: plan.version }) }); const body = await response.json().catch(() => ({})); setBusy(false); if (!response.ok) return setMessage(body.error || "Talep iletilemedi."); setPlan({ ...plan, status: "CHANGE_REQUESTED", version: plan.version + 1 }); setMessage("Değişiklik isteğin öğretmenine iletildi. Tercihlerini güncelleyip yeni öneri oluşturabilirsin."); }
  return <div className="grid gap-5 xl:grid-cols-[360px_minmax(0,1fr)]"><section className="panel-surface p-5"><div className="flex items-center gap-2"><SlidersHorizontal size={17} className="text-[var(--brand-olive)]" /><h2 className="text-sm font-extrabold">Benim kapasitem</h2></div><p className="mt-2 text-xs leading-5 text-[var(--site-muted)]">Daha çok iş değil, sürdürülebilir bir hafta seç.</p><div className="mt-5"><span className="panel-label">Uygun günler</span><div className="mt-2 grid grid-cols-4 gap-2">{days.map((day) => <button key={day.id} type="button" aria-pressed={preference.availableDays.includes(day.id)} onClick={() => setPreference((current) => ({ ...current, availableDays: current.availableDays.includes(day.id) ? current.availableDays.filter((id) => id !== day.id) : [...current.availableDays, day.id].sort() }))} className={`rounded-xl px-2 py-2 text-xs font-bold ${preference.availableDays.includes(day.id) ? "bg-[var(--brand-olive)] text-white" : "bg-[var(--site-bg-warm)]"}`}>{day.label}</button>)}</div></div><label className="mt-4 block"><span className="panel-label">Bir günde ayırabileceğim süre</span><select className="panel-input mt-2" value={preference.minutesPerDay} onChange={(event) => setPreference({ ...preference, minutesPerDay: Number(event.target.value) })}>{[20, 30, 45, 60, 90].map((value) => <option key={value} value={value}>{value} dakika</option>)}</select></label><div className="mt-4 grid grid-cols-2 gap-2"><label><span className="panel-label">Yaklaşan sınav</span><select className="panel-input mt-2" value={preference.examLabel || ""} onChange={(event) => setPreference({ ...preference, examLabel: event.target.value || null })}><option value="">Yok</option>{["LGS", "TYT", "AYT", "YDT", "OKUL SINAVI"].map((value) => <option key={value}>{value}</option>)}</select></label><label><span className="panel-label">Tarih</span><input type="date" className="panel-input mt-2" disabled={!preference.examLabel} value={preference.nextExamAt?.slice(0, 10) || ""} onChange={(event) => setPreference({ ...preference, nextExamAt: event.target.value || null })} /></label></div><label className="mt-4 block"><span className="panel-label">Bu yoğunluk bana nasıl geliyor?</span><select className="panel-input mt-2" value={preference.overwhelmPulse || ""} onChange={(event) => setPreference({ ...preference, overwhelmPulse: event.target.value ? Number(event.target.value) : null })}><option value="">Yanıtlamak istemiyorum</option><option value="1">Çok rahat</option><option value="2">Rahat</option><option value="3">Dengeli</option><option value="4">Biraz fazla</option><option value="5">Fazla</option></select></label><label className="mt-4 flex items-start gap-3 rounded-xl bg-[var(--site-bg-warm)] p-3 text-xs"><input type="checkbox" checked={preference.planningEnabled} onChange={(event) => setPreference({ ...preference, planningEnabled: event.target.checked })} /><span><strong className="block">Haftalık plan önerisi açık</strong><span className="mt-1 block text-[var(--site-muted)]">İstediğin zaman kapatabilirsin; mevcut akademik kayıtların silinmez.</span></span></label><button type="button" disabled={busy || !preference.availableDays.length} onClick={() => void savePreference()} className="panel-quick-action panel-quick-action-primary mt-4 w-full justify-center"><Check size={14} /> Tercihleri kaydet</button></section><section className="panel-surface p-5 sm:p-6"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-xs font-extrabold uppercase tracking-[.07em] text-[var(--brand-olive)]">Bu haftanın önerisi</p><h2 className="mt-1 text-xl font-semibold">{plan ? plan.status === "APPROVED" ? "Öğretmenin onayladı" : plan.status === "CHANGE_REQUESTED" ? "Değişiklik bekleniyor" : "Öğretmen onayı bekleniyor" : "Henüz plan oluşturulmadı"}</h2></div><button type="button" disabled={busy || !preference.planningEnabled || plan?.status === "APPROVED"} onClick={() => void generate()} className="panel-quick-action"><RefreshCw size={14} /> {plan ? "Kalan haftayı dengele" : "Öneri oluştur"}</button></div>{plan?.tasks.length ? <div className="mt-5 space-y-3">{plan.tasks.map((task) => <article key={task.id} className={`rounded-2xl border p-4 ${task.status === "DONE" ? "border-emerald-200 bg-emerald-50/60" : "border-[var(--site-line)] bg-white"}`}><div className="flex items-start justify-between gap-3"><div><p className="flex items-center gap-1.5 text-[11px] font-bold text-[var(--site-muted)]"><CalendarDays size={13} /> {date.format(new Date(task.scheduledFor))} · {task.durationMinutes} dk</p><h3 className="mt-1 text-sm font-extrabold">{task.title}</h3><p className="mt-2 text-xs text-[var(--brand-olive)]">Neden: {reasons[task.reasonCode]}</p></div>{plan.status === "APPROVED" ? <button type="button" disabled={task.status === "DONE"} onClick={() => void complete(task)} className="panel-quick-action">{task.status === "DONE" ? "Tamamlandı" : "Tamamla"}</button> : null}</div></article>)}</div> : <div className="mt-6 rounded-2xl border border-dashed border-[var(--site-line)] p-6 text-center"><ListChecks className="mx-auto text-[var(--site-muted)]" /><p className="mt-2 text-sm font-bold">Açık ödev veya tekrar geldiğinde plan burada oluşur.</p></div>}{plan?.status === "APPROVED" ? <div className="mt-5 flex flex-col gap-2 rounded-2xl bg-[#fff9dc] p-4 sm:flex-row"><select value={changeCategory} onChange={(event) => setChangeCategory(event.target.value)} className="panel-input flex-1" aria-label="Plan değişiklik nedeni"><option value="TOO_MUCH">Plan fazla yoğun</option><option value="WRONG_DAYS">Günler bana uymuyor</option><option value="PRIORITY">Öncelik doğru görünmüyor</option><option value="OTHER">Başka bir neden</option></select><button type="button" disabled={busy} onClick={() => void requestChange()} className="panel-quick-action">Değişiklik iste</button></div> : null}<p aria-live="polite" className="mt-4 text-xs font-bold text-[var(--brand-olive)]">{message}</p>{preference.overwhelmPulse && preference.overwhelmPulse >= 4 ? <p className="mt-3 flex gap-2 rounded-xl bg-amber-50 p-3 text-xs text-amber-900"><CircleAlert size={15} className="shrink-0" />Planın fazla geldiğini belirttin. Gün veya süreyi azaltıp yeniden dengeleyebilirsin.</p> : null}</section></div>;
}
