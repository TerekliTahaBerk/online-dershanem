"use client";

import { useState } from "react";
import { Check, ExternalLink, PackageCheck } from "lucide-react";

type Row = { id: string; status: "PUBLISHED" | "COMPLETED"; lessonTitle: string; lessonDate: string; summaryTopic: string; summaryNextStep: string; checkpointPrompt: string; checkpointResponse: "NOT_YET" | "NEED_HELP" | "READY" | null; dueAt: string; items: { id: string; kind: "MATERIAL" | "ASSIGNMENT"; title: string; completed: boolean; href: string | null }[] };
export function StudentRecoveryPackages({ rows }: { rows: Row[] }) {
  const [busy, setBusy] = useState<string | null>(null); const [message, setMessage] = useState("");
  async function completeItem(packageId: string, itemId: string) { setBusy(itemId); const response = await fetch(`/api/panel/recovery-packages/${packageId}/items/${itemId}/complete`, { method: "POST" }); const body = await response.json().catch(() => ({})); if (!response.ok) { setMessage(body.error || "Adım kaydedilemedi."); setBusy(null); return; } window.location.reload(); }
  async function checkpoint(packageId: string, responseValue: "NOT_YET" | "NEED_HELP" | "READY") { setBusy(packageId); const response = await fetch(`/api/panel/recovery-packages/${packageId}/checkpoint`, { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ response: responseValue }) }); const body = await response.json().catch(() => ({})); if (!response.ok) { setMessage(body.error || "Mini kontrol kaydedilemedi."); setBusy(null); return; } window.location.reload(); }
  return <div><p aria-live="polite" className="mb-4 text-sm font-bold text-[var(--brand-olive)]">{message}</p><div className="space-y-4">{rows.map((row) => <article key={row.id} className="panel-surface p-5 sm:p-6"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-xs font-extrabold uppercase tracking-[.06em] text-[var(--brand-olive)]">{new Date(row.lessonDate).toLocaleDateString("tr-TR")} · {row.lessonTitle}</p><h2 className="mt-2 text-xl font-semibold">{row.summaryTopic}</h2></div><span className={`rounded-full px-3 py-1 text-[11px] font-extrabold ${row.status === "COMPLETED" ? "bg-emerald-100 text-emerald-800" : "bg-[#fff4cc] text-amber-900"}`}>{row.status === "COMPLETED" ? "Telafi tamamlandı" : "Küçük adımlar hazır"}</span></div><div className="mt-4 rounded-2xl bg-[#eef2df] p-4"><p className="text-[11px] font-extrabold uppercase text-[var(--brand-olive)]">Sonraki küçük adım</p><p className="mt-2 text-sm leading-6">{row.summaryNextStep}</p><p className="mt-2 text-[11px] text-[var(--site-muted)]">72 saat hedefi: {new Date(row.dueAt).toLocaleString("tr-TR")}. Bu bir ceza veya biriken görev listesi değildir.</p></div>{row.items.length ? <div className="mt-4 space-y-2">{row.items.map((item) => <div key={item.id} className="flex flex-col gap-3 rounded-2xl border border-[var(--site-line)] p-4 sm:flex-row sm:items-center sm:justify-between"><div><p className="text-[10px] font-extrabold uppercase text-[var(--site-muted)]">{item.kind === "MATERIAL" ? "Kaynak" : "Çalışma"}</p><p className="mt-1 text-sm font-extrabold">{item.title}</p></div><div className="flex gap-2">{item.href ? <a href={item.href} target={item.kind === "MATERIAL" ? "_blank" : undefined} rel="noreferrer" className="panel-quick-action">Aç <ExternalLink size={13} /></a> : <span className="text-xs text-[var(--site-muted)]">Kaynak artık aktif değil</span>}{row.status === "PUBLISHED" ? <button type="button" disabled={busy !== null || item.completed} onClick={() => completeItem(row.id, item.id)} className="panel-quick-action">{item.completed ? "İncelendi" : "İnceledim"} {item.completed ? <Check size={13} /> : null}</button> : null}</div></div>)}</div> : null}<div className="mt-5 border-t border-[var(--site-line)] pt-5"><p className="text-sm font-extrabold">Mini kontrol</p><p className="mt-1 text-sm leading-6 text-[var(--site-body)]">{row.checkpointPrompt}</p>{row.status === "PUBLISHED" ? <div className="mt-3 flex flex-wrap gap-2"><button type="button" disabled={busy !== null} onClick={() => checkpoint(row.id, "NOT_YET")} className="panel-quick-action">Henüz değil</button><button type="button" disabled={busy !== null} onClick={() => checkpoint(row.id, "NEED_HELP")} className="panel-quick-action">Bir örnek daha gerekli</button><button type="button" disabled={busy !== null} onClick={() => checkpoint(row.id, "READY")} className="panel-quick-action panel-quick-action-primary">Açıklayabiliyorum</button></div> : <p className="mt-2 text-xs font-bold text-emerald-700">Yanıtın kaydedildi; tüm küçük adımlar tamamlandı.</p>}</div></article>)}{!rows.length ? <div className="panel-surface p-10 text-center"><PackageCheck className="mx-auto text-[var(--site-muted)]" /><p className="mt-3 text-sm font-bold">Yayınlanmış telafi paketin yok.</p></div> : null}</div></div>;
}
