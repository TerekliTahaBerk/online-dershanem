import { ExternalLink, FileText, Library, Link2, Video } from "lucide-react";
import { prisma } from "@/lib/prisma";
import { requireRole } from "@/lib/auth/guards";
import { getPanelFeatureFlags } from "@/lib/panel-feature-flags";
import { PanelShell } from "@/components/panel/panel-shell";

export const dynamic = "force-dynamic";
export default async function StudentMaterialsPage() {
  const session = await requireRole("STUDENT");
  const flags = getPanelFeatureFlags();
  const accessibilityEnabled = flags.accessibilityProfile;
  const [profile, preference, networkPreference] = await Promise.all([
    prisma.studentProfile.findUnique({ where: { userId: session.userId }, select: { id: true } }),
    accessibilityEnabled ? prisma.accessibilityPreference.findUnique({ where: { userId: session.userId }, select: { captionsPreferred: true, transcriptPreferred: true } }) : null,
    flags.offlineMode ? prisma.networkPreference.findUnique({ where: { userId: session.userId }, select: { lowDataMode: true } }) : null,
  ]);
  const materials = profile ? await prisma.learningMaterial.findMany({ where: { isActive: true, group: { enrollments: { some: { studentId: profile.id, endedAt: null } } } }, orderBy: { createdAt: "desc" }, include: { group: { select: { name: true, subject: true } } } }) : [];
  const lowDataMode = Boolean(networkPreference?.lowDataMode);
  const score = (material: (typeof materials)[number]) => Number(Boolean(preference?.captionsPreferred && material.captionsAvailable)) + Number(Boolean(preference?.transcriptPreferred && material.transcript)) + Number(Boolean(lowDataMode && material.transcript)) * 3 + Number(Boolean(lowDataMode && material.kind === "LINK"));
  const ordered = [...materials].sort((a, b) => score(b) - score(a) || b.createdAt.getTime() - a.createdAt.getTime());
  const icons = { LINK: Link2, PDF: FileText, VIDEO: Video };
  return <PanelShell role={session.role} fullName={session.fullName} email={session.email}><header><p className="flex items-center gap-2 text-xs font-bold uppercase tracking-[.08em] text-[var(--brand-olive)]"><Library size={15} /> Kaynaklarım</p><h1 className="mt-2 text-3xl font-semibold tracking-[-.05em] text-[var(--site-ink)]">İhtiyacın olan her şey burada.</h1>{lowDataMode ? <p className="mt-2 text-sm font-bold text-[var(--brand-olive)]">Düşük veri açık: metin dökümleri ve bağlantılar önce; büyük dosyalar yalnız siz açarsanız yüklenir.</p> : preference?.captionsPreferred || preference?.transcriptPreferred ? <p className="mt-2 text-sm text-[var(--site-body)]">Altyazı ve metin tercihinle eşleşen kaynaklar önce gösterilir.</p> : null}</header><div className="mt-7 grid gap-4 md:grid-cols-2 xl:grid-cols-3">{ordered.map((material) => { const Icon = icons[material.kind]; const href = material.blobPathname ? `/api/panel/materials/${material.id}/file` : material.url; const preferred = score(material) > 0; const dataHeavy = material.kind === "VIDEO" || material.kind === "PDF"; return <article key={material.id} className="rounded-[24px] border border-[var(--site-line)] bg-white p-5 shadow-[var(--panel-card-shadow)]"><div className="flex items-start justify-between gap-2"><span className="grid h-11 w-11 place-items-center rounded-2xl bg-[var(--brand-olive-soft)] text-[var(--brand-olive)]"><Icon size={19} /></span>{preferred ? <span className="rounded-full bg-emerald-50 px-2 py-1 text-[9px] font-bold text-emerald-800">Tercihinle uyumlu</span> : null}</div><p className="mt-4 text-[10px] font-bold uppercase text-[var(--site-muted)]">{material.group.name} · {material.group.subject}</p><h2 className="mt-2 text-base font-extrabold text-[var(--site-ink)]">{material.title}</h2><p className="mt-2 min-h-10 text-xs leading-5 text-[var(--site-body)]">{material.description || "Öğretmeninin paylaştığı çalışma kaynağı."}</p><div className="mt-3 flex flex-wrap gap-1">{material.captionsAvailable ? <span className="rounded-full bg-sky-50 px-2 py-1 text-[9px] font-bold text-sky-800">Altyazı var</span> : null}{material.transcript ? <span className="rounded-full bg-emerald-50 px-2 py-1 text-[9px] font-bold text-emerald-800">Metin dökümü var</span> : null}</div>{material.transcript ? <details open={lowDataMode || undefined} className="mt-3 rounded-2xl border border-[var(--site-line)] p-3"><summary className="cursor-pointer text-xs font-bold">Metin dökümünü oku</summary><p className="mt-3 whitespace-pre-wrap text-xs leading-6 text-[var(--site-body)]">{material.transcript}</p></details> : null}<a href={href} target="_blank" rel="noreferrer" className="panel-quick-action panel-quick-action-primary mt-5 w-full justify-center">{lowDataMode && dataHeavy ? `${material.kind === "VIDEO" ? "Videoyu" : "PDF’i"} aç (veri kullanır)` : "Kaynağı aç"} <ExternalLink size={14} /></a></article>; })}{!ordered.length ? <p className="rounded-[24px] border border-dashed border-[var(--site-line)] p-10 text-center text-sm text-[var(--site-muted)] md:col-span-2 xl:col-span-3">Henüz paylaşılmış materyal yok.</p> : null}</div></PanelShell>;
}
